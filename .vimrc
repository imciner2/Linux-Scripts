"set nocompatible

" Enable syntax highlighting
syntax on

" Enable line numbers
set number

" Keep the indent of the line before if the indent style isn't known
set autoindent

" Set the tab width to be 4 spaces
set shiftwidth=4
set tabstop=4

" Set the colorscheme
colorscheme default

" Add a line at 72 characters into the column
set colorcolumn=72
